sap:
  security:
    services:
      xsuaa: # default configuration of the test XSUAA server from SecurityTest
        xsappname: xsapp!t0815
        uaadomain: http://localhost:2222
        clientid: sb-clientId!t0815
        url: http://localhost
spring:
  main:
    allow-bean-definition-overriding: true